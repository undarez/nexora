# v2 tests

Memory: hash mismatch creates integrity event; rollback preserves history; quarantined/deprecated knowledge excluded.
Provenance: validated knowledge has evidence pointing to a registered source.
Retrieval: validated-only by default; confidence threshold and authority ranking apply; run/step linkage exists.
Decision gate: missing authorization BLOCK; critical/high irreversible REQUIRE_APPROVAL; low-risk authorized reversible may ALLOW.
Security: prompt injection cannot alter policy memory; external content cannot create authorization; secrets are rejected; conflicts are flagged.
Behaviour: tool failures, retries, policy blocks, approval requests, memory mutations, drift and evidence mismatches are recorded.
