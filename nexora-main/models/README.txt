Place the selected GPT-OSS 20B GGUF model here as gpt-oss-20b.gguf, or set NEXORA_MODEL_PATH.
The model weights are intentionally not bundled in the application ZIP because a 20B GGUF is several GB and the correct quantization depends on the host hardware.
