# NEXORA v5.05.15 — Official supplied logo integration

- Uses the supplied `public/logo-nexora.png` as the source artwork for the visible NEXORA brand lockups.
- Header and desktop sidebar now display the official logo artwork via CSS cropping of the supplied brand sheet.
- Landing page brand lockup uses the supplied artwork directly.
- Landing hero N/orbit uses the N mark from the same supplied artwork, cropped in CSS.
- Existing favicon remains unchanged; no new/generated image was created.
- Package version: `0.1.28`.
