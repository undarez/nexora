# NEXORA V5.08.33 — LIA Advanced Financial Reasoning

Adds deterministic signals for LIA: multi-month trends, recurring observed expenses and outlier/anomaly candidates. These are observations, not causal claims or guaranteed forecasts.

No Supabase migration is required.
Run `npm run lia:advanced-reasoning`, then `npm run typecheck` and `npm run build`.
